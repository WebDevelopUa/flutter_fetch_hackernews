import 'package:flutter/material.dart';

class App extends StatelessWidget {
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Fetch Hacker News App',
      home: Text('Hacker News'),
    );
  }
}

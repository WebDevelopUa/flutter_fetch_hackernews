name: flutter_fetch_hackernews
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  sqflite: ^2.0.0+3
  path_provider: ^2.0.1
  http: ^0.13.1
  rxdart: ^0.26.0

dev_dependencies:
  test: ^1.16.8

flutter:

  uses-material-design: true
